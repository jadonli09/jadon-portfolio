// WebGPU globals for the hero flare pipeline. `@webgpu/types` lives outside the
// `@types/*` scope, so it is not auto-included; referencing it here avoids a
// `types` array in tsconfig, which would switch off auto-inclusion for the rest.
/// <reference types="@webgpu/types" />
